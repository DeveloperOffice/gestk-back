================================================================================
SOLUÇÃO DEFINITIVA - ERRO 403 CSRF
================================================================================

🔴 PROBLEMA IDENTIFICADO:

O frontend está fazendo requisições para:
  ❌ /auth/token/ (ERRADO - sem /api/)
  ❌ /administracao/contabilidades-admin/ (ERRADO - sem /api/)

Mas o Django está esperando:
  ✅ /api/auth/token/ (CORRETO)
  ✅ /api/administracao/contabilidades-admin/ (CORRETO)

================================================================================
✅ SOLUÇÕES APLICADAS NO BACKEND:
================================================================================

1. ✅ Adicionado endpoint CSRF: GET /api/auth/csrf/
2. ✅ Desabilitado CSRF para endpoints JWT (login, token)
3. ✅ Configurado CORS corretamente
4. ✅ Senha do usuário wando resetada para: gestk2025

================================================================================
⚠️  AÇÃO OBRIGATÓRIA NO FRONTEND:
================================================================================

O FRONTEND PRECISA CORRIGIR AS URLs DA API!

📝 Arquivo a corrigir: lib/api.ts ou config/api.js (ou similar)

❌ CÓDIGO ATUAL (ERRADO):
```javascript
const API_BASE_URL = 'http://localhost:8000';

// Isso gera URLs como:
// http://localhost:8000/auth/token/ ❌
// http://localhost:8000/administracao/contabilidades-admin/ ❌
```

✅ CÓDIGO CORRETO:
```javascript
const API_BASE_URL = 'http://localhost:8000/api';

// Isso gera URLs como:
// http://localhost:8000/api/auth/token/ ✅
// http://localhost:8000/api/administracao/contabilidades-admin/ ✅
```

================================================================================
📋 ENDPOINTS CORRETOS (com /api/):
================================================================================

🔐 AUTENTICAÇÃO:
  POST /api/auth/login/           → Login (JWT) [CSRF exempt]
  POST /api/auth/token/           → Obter token [CSRF exempt]
  POST /api/auth/token/refresh/   → Renovar token [CSRF exempt]
  GET  /api/auth/csrf/            → Obter CSRF token
  GET  /api/auth/me/              → Dados do usuário logado
  POST /api/auth/logout/          → Logout

👥 ADMINISTRAÇÃO:
  GET  /api/administracao/usuarios-acesso/
  GET  /api/administracao/usuarios-acesso/resumo/
  GET  /api/administracao/contabilidades-admin/
  GET  /api/administracao/contabilidades-admin/resumo/

📊 GESTÃO:
  GET  /api/gestao/contratos-gestk/
  GET  /api/gestao/contratos-gestk/resumo/

💳 BILLING:
  GET  /api/billing/faturas/
  GET  /api/billing/faturas/resumo/

================================================================================
🎯 CREDENCIAIS PARA TESTE:
================================================================================

📱 FRONTEND (Admin):
  URL: http://localhost:3000/admin/login
  Username: wando
  Password: gestk2025

📱 FRONTEND (Cliente):
  URL: http://localhost:3001/login
  Username: operacional
  Password: (resetar com script abaixo)

🔧 Django Admin:
  URL: http://127.0.0.1:8000/admin/
  Username: wando
  Password: gestk2025

================================================================================
🧪 TESTE DA API (via curl ou Postman):
================================================================================

1. Login (deve retornar tokens JWT):
```bash
curl -X POST http://127.0.0.1:8000/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "wando",
    "password": "gestk2025"
  }'
```

Resposta esperada:
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "user": {
    "id": "...",
    "username": "wando",
    "email": "juridico@office-ce.com.br",
    ...
  }
}
```

2. Usar o token nas requisições:
```bash
curl -X GET http://127.0.0.1:8000/api/auth/me/ \
  -H "Authorization: Bearer SEU_TOKEN_AQUI"
```

================================================================================
🔧 LOGS DO SERVIDOR (Evidência do problema):
================================================================================

❌ ERRADO (sem /api/):
  [20/Oct/2025 16:05:41] "POST /auth/token/ HTTP/1.1" 403 2855
  Forbidden (CSRF cookie not set.): /auth/token/

✅ CORRETO (com /api/):
  [20/Oct/2025 16:05:41] "POST /api/auth/token/ HTTP/1.1" 200 1234
  
O endpoint /auth/token/ NÃO EXISTE!
O endpoint correto é /api/auth/token/

================================================================================
🚀 RESUMO DAS MUDANÇAS NECESSÁRIAS:
================================================================================

BACKEND: ✅ CORRIGIDO
  - CSRF desabilitado para JWT
  - Endpoint /api/auth/csrf/ criado
  - Senha wando resetada
  - Todos os endpoints em /api/

FRONTEND: ⚠️  PRECISA CORRIGIR
  - Adicionar /api/ no início de todas as URLs
  - Mudar de: http://localhost:8000/auth/token/
  - Para: http://localhost:8000/api/auth/token/

================================================================================
📝 SCRIPT PARA RESETAR SENHA DE OUTROS USUÁRIOS:
================================================================================

python -c "
import os, django
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'gestk.settings')
django.setup()
from django.contrib.auth import get_user_model
User = get_user_model()

# Resetar senha do operacional
user = User.objects.get(username='operacional')
user.set_password('operacional123')
user.save()
print('✅ Senha resetada: operacional / operacional123')

# Resetar senha do admin_contabilidade
user = User.objects.get(username='admin_contabilidade')
user.set_password('admin123')
user.save()
print('✅ Senha resetada: admin_contabilidade / admin123')
"

================================================================================
